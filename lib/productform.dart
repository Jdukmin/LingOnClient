import 'package:flutter/material.dart';

class PayFormRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Scaffold is a layout for the major Material Components.
    return Scaffold(
      appBar: AppBar(
      ),
      
      // body is the majority of the screen.
      body: Center(

      ),

      drawer: Drawer(
      ),
    );
  }
}